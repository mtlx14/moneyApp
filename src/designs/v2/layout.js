// Medidas compartidas del diseño v2.

// Ancho de la barra vertical de la izquierda.
export const NAV_WIDTH = 34;

// Espacio desde el borde izquierdo hasta donde empieza el contenido. Reemplaza
// al padding que traían las páginas: no se suma al ancho de la barra, lo
// incluye. La diferencia con NAV_WIDTH es el aire que queda.
export const CONTENT_LEFT = 37;

// Inicio va un poco más adentro que el resto.
export const CONTENT_LEFT_HOME = 47;

// Pinta los contenedores de la lista del mes siguiente. Solo para depurar.
export const DEBUG_LAYOUT = true;
